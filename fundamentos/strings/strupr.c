#include <stdio.h>
#include <string.h>

void main () {
    char nome[] = "Gustavo";
    printf("%s", strupr(nome)); //deixa tudo em uppercase (maisuculo)
    //printf("%s", strlwr(nome)); deixa tudo em lowercase (minusculo)
}